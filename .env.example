# Postgres connection (via flyctl proxy)
POSTGRES_PROXY_HOST=localhost
POSTGRES_PROXY_PORT=5432
POSTGRES_DB=cinderhaven
POSTGRES_USER=postgres
POSTGRES_PASSWORD=

# dbt uses POSTGRES_PASSWORD via env_var() in profiles.yml
